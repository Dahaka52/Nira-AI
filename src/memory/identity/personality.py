"""
Устойчивые черты характера Нира.

Определяет поведенческие паттерны, стиль общения,
реакции — то, что не меняется от диалога к диалогу.
Обновляется медленно, только через значимые события.

TODO:
- load() — загрузить текущие черты из хранилища
- get_as_prompt() → str — сформировать промпт-фрагмент с описанием характера
- update_trait(name, value) — скорректировать черту (медленно, с логированием)

Текущие базовые черты (из nira.md):
- жизнерадостность и любопытность
- самоирония
- живой разговорный язык
- интерес к играм и ролевым играм
- стремление прокачиваться вместе с пользователем
"""
from typing import Dict, Any


# Базовые черты характера — статичная инициализация
BASE_PERSONALITY: Dict[str, Any] = {
    "жизнерадостность": 0.9,
    "любопытность": 0.9,
    "самоирония": 0.8,
    "разговорность": 0.85,
    "эмоциональность": 0.7,
    "формальность": 0.1,  # очень низкая — Нира говорит живо
}


class PersonalityModel:
    """Заготовка. Не активна."""

    def __init__(self):
        self.traits: Dict[str, float] = dict(BASE_PERSONALITY)

    def get_as_prompt(self) -> str:
        """Сформировать текстовое описание характера для промпта."""
        raise NotImplementedError

    def update_trait(self, name: str, delta: float, reason: str = ""):
        """Скорректировать черту характера (медленно, с ограничением)."""
        raise NotImplementedError
